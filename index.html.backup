<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>RoomGlow — AI Room Aesthetic Analysis</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style>
    :root {
      --bg: #0a0a0a;
      --surface: #111111;
      --border: #222222;
      --gold: #f0a500;
      --amber: #ff6b35;
      --text: #f5f5f5;
      --muted: #888888;
      --success: #4caf50;
      --danger: #e53935;
      --warn: #fbc02d;
      --grad: linear-gradient(135deg, #f0a500, #ff6b35);
      --shadow: 0 4px 24px rgba(240, 165, 0, 0.08);
      --shadow-hover: 0 12px 40px rgba(240, 165, 0, 0.15);
      --radius: 16px;
      --pill: 100px;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: Inter, system-ui, sans-serif;
      background: var(--bg);
      color: var(--text);
      overflow-x: hidden;
      line-height: 1.5;
      opacity: 0;
      animation: pageIn 0.6s ease forwards;
    }
    @keyframes pageIn { to { opacity: 1; } }

    a { color: inherit; text-decoration: none; }
    img { max-width: 100%; display: block; }

    .container { width: min(1180px, 100% - 40px); margin: 0 auto; }
    .section-alt { background: #111111; }
    .section-muted { background: #0d0d0d; }

    /* Gold glow on interactive hover */
    button, .rg-btn, .rg-navlink, .rg-pill, .rg-room-pill, .rg-filter-pill, .rg-faq-q, summary, a.rg-link {
      transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease, background 0.2s ease;
    }
    button:hover, .rg-btn:hover:not(:disabled), .rg-navlink:hover { transform: scale(1.02); }
    .rg-card:hover, .rg-step-card:hover, .rg-ex-card:hover, .rg-prod-card:hover, .rg-testi-card:hover, .rg-pricing-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-hover);
    }

    /* --- Nav --- */
    .rg-nav {
      position: sticky;
      top: 0;
      z-index: 1000;
      background: rgba(10, 10, 10, 0.8);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid transparent;
      border-image: linear-gradient(90deg, transparent, var(--gold), var(--amber), transparent) 1;
      transition: padding 0.25s ease;
    }
    .rg-nav.rg-nav--compact { padding-top: 8px; padding-bottom: 8px; }
    .rg-nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.85rem 0;
      max-width: 1180px;
      margin: 0 auto;
      width: calc(100% - 40px);
    }
    .rg-logo { display: flex; align-items: center; gap: 0.5rem; font-weight: 800; font-size: 22px; color: var(--text); }
    .rg-nav-links { display: flex; gap: 1.75rem; flex-wrap: wrap; justify-content: center; flex: 1; }
    .rg-navlink { font-size: 14px; font-weight: 500; color: var(--muted); padding: 0.35rem 0; }
    .rg-navlink:hover { color: var(--text); }
    .rg-nav-cta {
      background: var(--grad);
      color: #0a0a0a;
      font-weight: 700;
      padding: 0.65rem 1.25rem;
      border-radius: var(--pill);
      border: none;
      cursor: pointer;
      font-size: 14px;
    }
    .rg-hamburger {
      display: none;
      background: var(--surface);
      border: 1px solid var(--border);
      color: var(--text);
      width: 44px; height: 44px;
      border-radius: var(--radius);
      cursor: pointer;
      font-size: 22px;
    }
    .rg-menu-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.92);
      z-index: 2000;
      flex-direction: column;
      padding: 2rem;
      gap: 1.5rem;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    .rg-menu-overlay.open { display: flex; opacity: 1; }
    .rg-menu-overlay a { font-size: 1.25rem; font-weight: 600; color: var(--text); padding: 0.75rem 0; border-bottom: 1px solid var(--border); }
    .rg-menu-close { align-self: flex-end; background: none; border: none; color: var(--text); font-size: 2rem; cursor: pointer; }

    @media (max-width: 900px) {
      .rg-nav-links { display: none; }
      .rg-hamburger { display: grid; place-items: center; }
    }

    /* --- Hero --- */
    .rg-hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      position: relative;
      padding: 6rem 1.25rem 4rem;
      overflow: hidden;
    }
    .rg-hero-bg { position: absolute; inset: 0; pointer-events: none; }
    .rg-orb { position: absolute; border-radius: 50%; filter: blur(80px); opacity: 0.52; animation: floatOrb 18s ease-in-out infinite; }
    .rg-orb1 { width: 420px; height: 420px; background: radial-gradient(circle, #f0a50088, transparent 70%); top: -10%; left: -5%; }
    .rg-orb2 { width: 380px; height: 380px; background: radial-gradient(circle, #ff6b3588, transparent 70%); top: 30%; right: -8%; animation-delay: -6s; }
    .rg-orb3 { width: 320px; height: 320px; background: radial-gradient(circle, #d8431588, transparent 70%); bottom: 5%; left: 25%; animation-delay: -12s; }
    @keyframes floatOrb { 0%, 100% { transform: translate(0,0) scale(1); } 50% { transform: translate(20px, -25px) scale(1.05); } }
    .rg-grain {
      position: absolute; inset: 0; opacity: 0.07; pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    }
    .rg-hero-inner { position: relative; z-index: 2; text-align: center; max-width: 900px; }
    .rg-badge {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.5rem 1rem; border-radius: var(--pill);
      background: rgba(240, 165, 0, 0.12); border: 1px solid rgba(240, 165, 0, 0.35);
      color: var(--gold); font-size: 13px; font-weight: 600; margin-bottom: 1.5rem;
      animation: badgePulse 3s ease-in-out infinite;
    }
    @keyframes badgePulse { 0%, 100% { box-shadow: 0 0 0 0 rgba(240,165,0,0.2); } 50% { box-shadow: 0 0 20px 2px rgba(240,165,0,0.15); } }
    .rg-hero h1 {
      font-size: clamp(40px, 6vw, 72px);
      font-weight: 800;
      letter-spacing: -0.03em;
      line-height: 1.08;
      margin-bottom: 1rem;
    }
    .rg-hero-sub { font-size: clamp(16px, 2vw, 20px); color: var(--muted); max-width: 720px; margin: 0 auto 2rem; }
    .rg-hero-ctas { display: flex; flex-wrap: wrap; gap: 1rem; justify-content: center; margin-bottom: 1rem; }
    .rg-live-counter { font-size: 13px; color: var(--muted); margin-bottom: 1.75rem; }
    .rg-live-counter span { font-weight: 700; color: var(--gold); }
    .rg-hero-mockup {
      position: relative;
      margin: 0 auto 2rem;
      max-width: 320px;
      animation: mockFloat 4s ease-in-out infinite;
    }
    @keyframes mockFloat { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
    .rg-mockup-glow {
      position: absolute; inset: -20px -30px -10px -30px;
      background: radial-gradient(ellipse at center, rgba(240,165,0,0.22), transparent 65%);
      pointer-events: none; z-index: 0;
    }
    .rg-mockup-card {
      position: relative; z-index: 1;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.25rem 1.5rem;
      box-shadow: 0 8px 40px rgba(240,165,0,0.12), var(--shadow);
    }
    .rg-mockup-score {
      position: relative;
      isolation: isolate;
      width: 100px; height: 100px; margin: 0 auto;
      border-radius: 50%;
      background: conic-gradient(var(--gold) 73%, #2a2a2a 0);
      display: grid; place-items: center;
      padding: 7px;
    }
    .rg-mockup-score::after {
      content: ""; position: absolute; inset: 7px; border-radius: 50%; background: var(--surface);
    }
    .rg-mockup-score .rg-mockup-num {
      position: relative; z-index: 1;
      font-size: 20px; font-weight: 800;
      background: var(--grad); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
    }
    .rg-mockup-score .rg-mockup-den { font-size: 0.55em; opacity: 0.85; }
    .rg-mockup-pill {
      text-align: center; margin-top: 0.75rem;
      display: inline-block; width: 100%;
      padding: 0.4rem 0.75rem; border-radius: var(--pill);
      background: rgba(240, 165, 0, 0.12); border: 1px solid rgba(240, 165, 0, 0.35);
      color: var(--gold); font-weight: 700; font-size: 13px;
    }
    .rg-mockup-tips { margin-top: 1rem; display: flex; flex-direction: column; gap: 0.5rem; }
    .rg-mockup-tip {
      background: #1a1a1a; border: 1px solid var(--border); border-radius: 10px;
      padding: 0.5rem 0.65rem; font-size: 12px; color: var(--muted); text-align: left;
    }
    @media (max-width: 479px) { .rg-hero-mockup { display: none; } }
    .rg-btn-primary {
      background: var(--grad);
      color: #0a0a0a;
      font-weight: 800;
      padding: 1rem 1.75rem;
      border-radius: var(--pill);
      border: none;
      cursor: pointer;
      font-size: 17px;
    }
    .rg-btn-outline {
      background: transparent;
      color: var(--text);
      border: 2px solid var(--border);
      font-weight: 700;
      padding: 1rem 1.75rem;
      border-radius: var(--pill);
      cursor: pointer;
      font-size: 17px;
    }
    .rg-social-proof { display: flex; align-items: center; justify-content: center; gap: 1rem; flex-wrap: wrap; color: var(--muted); font-size: 14px; }
    .rg-avatars { display: flex; }
    .rg-avatar {
      width: 40px; height: 40px; border-radius: 50%;
      background: linear-gradient(135deg, #333, #555);
      border: 2px solid var(--bg);
      margin-left: -12px;
    }
    .rg-avatar:first-child { margin-left: 0; }
    .rg-scroll-hint {
      position: absolute; bottom: 1.5rem; left: 50%; transform: translateX(-50%);
      font-size: 1.5rem; color: var(--muted); animation: bounce 2s ease infinite;
    }
    @keyframes bounce { 0%, 100% { transform: translateX(-50%) translateY(0); } 50% { transform: translateX(-50%) translateY(8px); } }

    /* Trust bar */
    .rg-trust {
      position: relative;
      overflow: hidden;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 1rem 0;
      background: #0a0a0a;
    }
    .rg-trust-shimmer {
      position: absolute; inset: 0;
      background: linear-gradient(90deg, transparent, rgba(240,165,0,0.06), transparent);
      background-size: 200% 100%;
      animation: shimmer 4s linear infinite;
      pointer-events: none;
    }
    @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }
    .rg-trust-row { display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 0; position: relative; z-index: 1; }
    .rg-trust-item { padding: 0.5rem 1.5rem; font-size: 14px; font-weight: 600; color: var(--muted); display: flex; align-items: center; gap: 0.5rem; }
    .rg-trust-div { width: 1px; height: 24px; background: var(--border); }
    @media (max-width: 768px) { .rg-trust-div { display: none; } .rg-trust-row { flex-direction: column; gap: 0.75rem; } }

    /* Section titles */
    .rg-section { padding: 5rem 1.25rem; }
    .rg-pill-label {
      display: inline-block; padding: 0.4rem 1rem; border-radius: var(--pill);
      background: rgba(240, 165, 0, 0.1); border: 1px solid rgba(240, 165, 0, 0.25);
      color: var(--gold); font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;
      margin-bottom: 1rem;
    }
    .rg-sec-title { font-size: clamp(28px, 4vw, 42px); font-weight: 800; letter-spacing: -0.02em; margin-bottom: 0.75rem; }
    .rg-sec-sub { color: var(--muted); font-size: 18px; max-width: 560px; margin-bottom: 2rem; }

    /* How it works */
    .rg-steps-wrap { position: relative; }
    .rg-steps {
      display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem;
    }
    @media (max-width: 900px) { .rg-steps { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 520px) { .rg-steps { grid-template-columns: 1fr; } }
    .rg-step-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.5rem;
      box-shadow: var(--shadow);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .rg-step-num { font-size: 48px; font-weight: 800; background: var(--grad); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; line-height: 1; }
    .rg-step-icon { font-size: 28px; margin: 0.5rem 0; }
    .rg-step-card h3 { font-size: 17px; margin-bottom: 0.5rem; }
    .rg-step-card p { font-size: 14px; color: var(--muted); }
    .rg-reveal { opacity: 0; transform: translateY(28px); transition: opacity 0.6s ease, transform 0.6s ease; }
    .rg-reveal.visible { opacity: 1; transform: translateY(0); }

    /* Analyzer */
    .rg-room-row { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.5rem; justify-content: center; }
    .rg-room-pill {
      padding: 0.55rem 1rem; border-radius: var(--pill);
      background: var(--surface); border: 1px solid var(--border);
      color: var(--muted); font-size: 13px; font-weight: 600; cursor: pointer;
    }
    .rg-room-pill.is-active { background: var(--grad); color: #0a0a0a; border-color: transparent; }

    .rg-upload-card {
      background: var(--surface);
      border: 2px dashed rgba(240, 165, 0, 0.45);
      border-radius: var(--radius);
      padding: 2rem;
      min-height: 280px;
      position: relative;
      box-shadow: var(--shadow);
      transition: border-color 0.2s ease;
    }
    .rg-upload-card.rg-drag { border-color: var(--gold); }
    .rg-upload-card-inner { text-align: center; cursor: pointer; }
    .rg-upload-placeholder { padding: 2rem 0; }
    .rg-upload-placeholder.hidden { display: none; }
    .rg-preview-wrap { position: relative; display: none; border-radius: var(--radius); overflow: hidden; }
    .rg-preview-wrap.visible { display: block; animation: fadeIn 0.4s ease; }
    @keyframes fadeIn { from { opacity: 0; transform: scale(0.98); } to { opacity: 1; transform: scale(1); } }
    .rg-preview-wrap img { width: 100%; max-height: 420px; object-fit: cover; }
    .rg-change-photo {
      position: absolute; top: 12px; right: 12px;
      background: rgba(0,0,0,0.75); color: var(--text);
      border: 1px solid var(--border); padding: 0.5rem 1rem; border-radius: var(--pill);
      font-size: 13px; font-weight: 600; cursor: pointer;
    }
    .rg-skeleton { display: none; position: absolute; inset: 0; border-radius: var(--radius); background: linear-gradient(90deg, #1a1a1a 25%, #252525 50%, #1a1a1a 75%); background-size: 200% 100%; animation: skel 1.2s ease infinite; }
    .rg-skeleton.active { display: block; }
    @keyframes skel { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }

    .rg-analyze-btn {
      width: 100%; margin-top: 1.25rem;
      background: var(--grad);
      color: #0a0a0a;
      font-weight: 800;
      font-size: 18px;
      padding: 1.1rem;
      border: none;
      border-radius: var(--pill);
      cursor: pointer;
      display: flex; align-items: center; justify-content: center; gap: 0.5rem;
      min-height: 56px;
    }
    .rg-analyze-btn:disabled { opacity: 0.65; cursor: not-allowed; transform: none; }
    .rg-analyze-btn.loading {
      animation: btnPulse 1.2s ease-in-out infinite;
    }
    @keyframes btnPulse { 0%, 100% { box-shadow: 0 0 0 0 rgba(240,165,0,0.25); } 50% { box-shadow: 0 0 24px 4px rgba(240,165,0,0.15); } }
    .rg-analyze-btn .rg-load-inner { display: none; align-items: center; gap: 0.75rem; }
    .rg-analyze-btn.loading .rg-btn-text { display: none; }
    .rg-analyze-btn.loading .rg-load-inner { display: flex; }
    .rg-spinner { width: 22px; height: 22px; border: 3px solid rgba(10,10,10,0.2); border-top-color: #0a0a0a; border-radius: 50%; animation: spin 0.7s linear infinite; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .rg-pulse-text { animation: pulseOp 1.2s ease-in-out infinite; }
    @keyframes pulseOp { 0%, 100% { opacity: 0.9; } 50% { opacity: 0.5; } }

    /* Results */
    .rg-results { display: none; margin-top: 3rem; }
    .rg-results.visible { display: block; }
    .rg-score-wrap { display: flex; flex-direction: column; align-items: center; margin-bottom: 2rem; }
    .rg-score-ring {
      width: 180px; height: 180px; border-radius: 50%;
      display: grid; place-items: center;
      position: relative;
      background: conic-gradient(var(--ring-color, var(--success)) calc(var(--pct, 0) * 1%), #222 0);
      padding: 8px;
    }
    .rg-score-ring::before {
      content: ""; position: absolute; inset: 8px; border-radius: 50%; background: var(--surface);
    }
    .rg-score-ring span { position: relative; z-index: 1; font-size: 42px; font-weight: 800; }
    .rg-style-pill {
      margin-top: 1rem; padding: 0.5rem 1.25rem; border-radius: var(--pill);
      background: rgba(240, 165, 0, 0.15); border: 1px solid rgba(240, 165, 0, 0.4);
      color: var(--gold); font-weight: 700; font-size: 15px;
    }
    .rg-hint { color: var(--muted); margin-top: 0.75rem; font-size: 15px; }

    .rg-tip-list { display: flex; flex-direction: column; gap: 1rem; }
    .rg-tip-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.25rem;
      box-shadow: var(--shadow);
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 1rem;
      align-items: start;
      opacity: 0;
      transform: translateY(18px);
      animation: tipIn 0.45s ease forwards;
    }
    @keyframes tipIn { to { opacity: 1; transform: translateY(0); } }
    .rg-tip-card .rg-cat { grid-column: 1 / -1; }
    .rg-tip-icon {
      width: 52px; height: 52px; border-radius: 50%;
      background: rgba(240, 165, 0, 0.12); border: 1px solid rgba(240, 165, 0, 0.35);
      display: grid; place-items: center; font-size: 24px;
    }
    .rg-cat-pill { display: inline-block; font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.04em; color: var(--muted); background: #1a1a1a; padding: 0.25rem 0.6rem; border-radius: var(--pill); border: 1px solid var(--border); }
    .rg-tip-text { font-size: 15px; line-height: 1.55; }
    .rg-shop-btn {
      margin-top: 0.75rem; display: inline-flex; align-items: center; gap: 0.35rem;
      background: transparent; color: var(--gold); border: 1px solid rgba(240, 165, 0, 0.5);
      padding: 0.45rem 0.9rem; border-radius: var(--pill); font-weight: 700; font-size: 13px; cursor: pointer;
    }

    .rg-share-card {
      margin-top: 2rem;
      background: linear-gradient(145deg, #1a1a1a, #111);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2rem;
      text-align: center;
      box-shadow: var(--shadow);
    }
    .rg-share-card .rg-logo-small { font-size: 20px; font-weight: 800; margin-bottom: 0.5rem; }
    .rg-share-actions { display: flex; flex-wrap: wrap; gap: 0.75rem; justify-content: center; margin-top: 1.25rem; }
    .rg-btn-white { background: var(--text); color: #0a0a0a; font-weight: 800; padding: 0.85rem 1.5rem; border-radius: var(--pill); border: none; cursor: pointer; }
    .rg-btn-ghost { background: transparent; color: var(--text); border: 1px solid var(--border); font-weight: 700; padding: 0.85rem 1.5rem; border-radius: var(--pill); cursor: pointer; }

    /* Examples */
    .rg-filters { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.5rem; }
    .rg-filter-pill { padding: 0.45rem 0.9rem; border-radius: var(--pill); font-size: 13px; font-weight: 600; background: var(--surface); border: 1px solid var(--border); color: var(--muted); cursor: pointer; }
    .rg-filter-pill.is-active { background: var(--grad); color: #0a0a0a; border-color: transparent; }
    .rg-ex-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
    @media (max-width: 900px) { .rg-ex-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 520px) { .rg-ex-grid { grid-template-columns: 1fr; } }
    .rg-ex-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      position: relative;
    }
    .rg-ex-card.rg-hidden { display: none !important; }
    .rg-ex-card {
      transition: opacity 0.35s ease, transform 0.35s ease, box-shadow 0.2s ease;
    }
    .rg-ex-card.rg-ex-hiding {
      opacity: 0; transform: scale(0.96); pointer-events: none;
    }
    .rg-ex-icon {
      position: absolute; inset: 0; display: grid; place-items: center;
      font-size: 2.5rem; opacity: 0.35; pointer-events: none;
    }
    .rg-prod-emoji {
      height: 140px; display: grid; place-items: center; font-size: 3rem;
    }
    .rg-ex-img { height: 160px; position: relative; }
    .rg-ex-overlay {
      position: absolute; inset: 0; background: rgba(0,0,0,0.55);
      display: grid; place-items: center; opacity: 0; transition: opacity 0.2s ease;
      font-weight: 800; color: var(--text);
    }
    .rg-ex-card:hover .rg-ex-overlay { opacity: 1; }
    .rg-ex-meta { padding: 0.75rem 1rem; display: flex; justify-content: space-between; align-items: flex-end; gap: 0.5rem; flex-wrap: wrap; }
    .rg-badge-score { position: absolute; top: 10px; right: 10px; background: rgba(0,0,0,0.75); padding: 0.35rem 0.65rem; border-radius: var(--pill); font-size: 12px; font-weight: 800; color: var(--gold); border: 1px solid rgba(240,165,0,0.4); }

    /* Products */
    .rg-prod-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; }
    @media (max-width: 900px) { .rg-prod-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 520px) { .rg-prod-grid { grid-template-columns: 1fr; } }
    .rg-prod-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; box-shadow: var(--shadow); transition: transform 0.2s ease, box-shadow 0.2s ease; }
    .rg-prod-img { height: 140px; }
    .rg-prod-body { padding: 1rem; }
    .rg-prod-body h4 { font-size: 16px; margin-bottom: 0.35rem; }
    .rg-prod-body p { font-size: 13px; color: var(--muted); margin-bottom: 0.75rem; }
    .rg-prod-body a { margin-top: 0.5rem; display: inline-block; }

    /* Stats */
    .rg-stats-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; text-align: center; }
    @media (max-width: 768px) { .rg-stats-row { grid-template-columns: repeat(2, 1fr); } }
    .rg-stat-num { font-size: clamp(32px, 5vw, 48px); font-weight: 800; background: var(--grad); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
    .rg-stat-label { font-size: 14px; color: var(--muted); margin-top: 0.35rem; }

    /* Testimonials carousel */
    .rg-carousel-wrap { overflow: hidden; position: relative; padding: 0.5rem 0; }
    .rg-carousel-wrap:hover .rg-carousel-track { animation-play-state: paused; }
    .rg-carousel-track {
      display: flex; gap: 1.25rem;
      width: max-content;
      animation: scrollX 40s linear infinite;
    }
    @keyframes scrollX { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
    .rg-testi-card {
      flex: 0 0 320px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.5rem;
      box-shadow: var(--shadow);
    }
    .rg-stars { color: var(--gold); margin-bottom: 0.75rem; }
    .rg-testi-card blockquote { font-style: italic; color: var(--text); font-size: 14px; margin-bottom: 1rem; line-height: 1.55; }
    .rg-testi-card .rg-author { display: flex; align-items: center; gap: 0.75rem; }
    .rg-av { width: 44px; height: 44px; border-radius: 50%; background: linear-gradient(135deg, #333, #555); }

    /* Pricing */
    .rg-pricing-row { display: flex; flex-wrap: wrap; gap: 1.5rem; justify-content: center; align-items: stretch; }
    .rg-pricing-card {
      flex: 1 1 320px; max-width: 400px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2rem;
      box-shadow: var(--shadow);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .rg-pricing-card.rg-popular {
      border: 2px solid var(--gold);
      transform: scale(1.03);
      position: relative;
    }
    .rg-pop-badge {
      position: absolute; top: -12px; right: 1rem;
      background: var(--grad); color: #0a0a0a; font-size: 11px; font-weight: 800; padding: 0.35rem 0.75rem; border-radius: var(--pill);
    }
    .rg-price { font-size: 36px; font-weight: 800; margin: 0.5rem 0; }
    .rg-features { list-style: none; margin: 1.25rem 0; }
    .rg-features li { padding: 0.4rem 0; font-size: 14px; color: var(--muted); }

    /* FAQ */
    .rg-faq-item { border: 1px solid var(--border); border-radius: var(--radius); margin-bottom: 0.75rem; overflow: hidden; background: var(--surface); }
    .rg-faq-q { width: 100%; text-align: left; padding: 1.1rem 1.25rem; background: transparent; color: var(--text); font-size: 15px; font-weight: 600; cursor: pointer; display: flex; justify-content: space-between; align-items: center; gap: 1rem; border: none; }
    .rg-faq-a {
      display: grid;
      grid-template-rows: 0fr;
      transition: grid-template-rows 0.4s ease;
    }
    .rg-faq-item.open .rg-faq-a { grid-template-rows: 1fr; }
    .rg-faq-a-inner { overflow: hidden; min-height: 0; padding: 0 1.25rem 1.1rem; color: var(--muted); font-size: 14px; line-height: 1.6; }

    /* Final CTA */
    .rg-final-cta {
      padding: 5rem 1.25rem;
      background: linear-gradient(135deg, #c77a00, #ff6b35, #f0a500);
      background-size: 200% 200%;
      animation: gradMove 8s ease infinite;
      text-align: center;
    }
    @keyframes gradMove { 0%, 100% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } }
    .rg-final-cta h2 { font-size: clamp(28px, 4vw, 44px); color: #0a0a0a; font-weight: 800; margin-bottom: 0.75rem; }
    .rg-final-cta p { color: rgba(10,10,10,0.85); margin-bottom: 1.5rem; font-weight: 600; }
    .rg-final-cta .rg-btn-white { font-size: 17px; margin-bottom: 0.75rem; }

    /* Footer */
    footer.rg-footer {
      border-top: 2px solid transparent;
      border-image: linear-gradient(90deg, transparent, var(--gold), var(--amber), transparent) 1;
      padding: 3rem 1.25rem 2rem;
      background: #0a0a0a;
    }
    .rg-footer-top { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 1.5rem; align-items: center; padding-bottom: 2rem; border-bottom: 1px solid var(--border); }
    .rg-footer-top nav { display: flex; gap: 1.25rem; flex-wrap: wrap; justify-content: center; flex: 1; }
    .rg-footer-top nav a { color: var(--muted); font-size: 14px; }
    .rg-footer-top nav a:hover { color: var(--text); }
    .rg-tagline { color: var(--muted); font-size: 14px; max-width: 220px; text-align: right; }
    .rg-footer-bottom { display: flex; flex-wrap: wrap; justify-content: space-between; align-items: center; gap: 1rem; padding-top: 1.5rem; font-size: 13px; color: var(--muted); }
    @media (max-width: 768px) { .rg-footer-top { flex-direction: column; text-align: center; } .rg-tagline { text-align: center; } .rg-footer-bottom { flex-direction: column; text-align: center; } }

    .rg-error { color: #ff8a80; margin-top: 1rem; font-size: 14px; display: none; }
    .rg-error.visible { display: block; }

    /* Cursor glow on interactive */
    .rg-interactive { cursor: pointer; }
    .rg-interactive:hover { box-shadow: 0 0 0 1px rgba(240,165,0,0.25), 0 0 24px rgba(240,165,0,0.08); }

    /* Pro modal */
    .rg-modal {
      position: fixed; inset: 0; z-index: 5000;
      display: grid; place-items: center;
      padding: 1.5rem;
      opacity: 0; visibility: hidden; pointer-events: none;
      transition: opacity 0.3s ease, visibility 0.3s;
    }
    .rg-modal.open { opacity: 1; visibility: visible; pointer-events: auto; }
    .rg-modal-backdrop { position: absolute; inset: 0; background: rgba(0,0,0,0.75); backdrop-filter: blur(6px); }
    .rg-modal-card {
      position: relative; z-index: 1;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 2rem;
      max-width: 420px; width: 100%;
      box-shadow: var(--shadow-hover);
    }
    .rg-modal-close {
      position: absolute; top: 12px; right: 12px;
      background: transparent; border: none; color: var(--muted); font-size: 1.5rem; cursor: pointer; line-height: 1;
    }
    .rg-modal-card h3 { font-size: 1.35rem; margin-bottom: 0.5rem; }
    .rg-modal-card p { color: var(--muted); font-size: 14px; margin-bottom: 1rem; }
    .rg-modal-card input {
      width: 100%; padding: 0.85rem 1rem; border-radius: var(--radius);
      border: 1px solid var(--border); background: #0a0a0a; color: var(--text);
      font-size: 15px; margin-bottom: 1rem;
    }
    .rg-modal-msg { display: none; color: var(--success); font-weight: 700; font-size: 14px; margin-top: 0.75rem; }

    /* Toast */
    .rg-toast {
      position: fixed; bottom: 2rem; left: 50%; transform: translateX(-50%) translateY(80px);
      background: var(--surface); border: 1px solid var(--gold); color: var(--gold);
      padding: 0.65rem 1.25rem; border-radius: var(--pill); font-weight: 700; font-size: 14px;
      z-index: 6000; opacity: 0; transition: transform 0.35s ease, opacity 0.35s ease;
      pointer-events: none;
    }
    .rg-toast.show { transform: translateX(-50%) translateY(0); opacity: 1; }

    .rg-testi-av {
      width: 48px; height: 48px; border-radius: 50%; flex-shrink: 0;
      display: grid; place-items: center; font-weight: 800; font-size: 14px; color: #fff;
    }

    @media (max-width: 520px) {
      .rg-btn-primary, .rg-btn-outline, .rg-nav-cta, .rg-analyze-btn, .rg-btn-white, .rg-btn-ghost, .rg-pricing-row .rg-btn-outline, .rg-pricing-row .rg-nav-cta {
        width: 100%; justify-content: center;
      }
      .rg-hero-ctas { flex-direction: column; width: 100%; max-width: 360px; margin-left: auto; margin-right: auto; }
    }

    #home, #how, #analyze, #examples, #shop, #stats, #reviews, #pricing, #faq, #final-cta, #privacy, #terms {
      scroll-margin-top: 88px;
    }
  </style>
</head>
<body>
  <div id="rg-root">
    <!-- NAV -->
    <header class="rg-nav" id="rgNav">
      <div class="rg-nav-inner">
        <a href="#home" class="rg-logo">🔥 RoomGlow</a>
        <nav class="rg-nav-links">
          <a class="rg-navlink rg-anchor" href="#home">Home</a>
          <a class="rg-navlink rg-anchor" href="#how">How It Works</a>
          <a class="rg-navlink rg-anchor" href="#examples">Examples</a>
          <a class="rg-navlink rg-anchor" href="#pricing">Pricing</a>
          <a class="rg-navlink rg-anchor" href="#reviews">Reviews</a>
        </nav>
        <button type="button" class="rg-nav-cta" id="rgNavCta">Start Free →</button>
        <button type="button" class="rg-hamburger" id="rgMenuOpen" aria-label="Menu">☰</button>
      </div>
    </header>

    <div class="rg-menu-overlay" id="rgMenuOverlay">
      <button type="button" class="rg-menu-close" id="rgMenuClose">&times;</button>
      <a href="#home" class="rg-anchor">Home</a>
      <a href="#how" class="rg-anchor">How It Works</a>
      <a href="#examples" class="rg-anchor">Examples</a>
      <a href="#pricing" class="rg-anchor">Pricing</a>
      <a href="#reviews" class="rg-anchor">Reviews</a>
      <button type="button" class="rg-nav-cta" id="rgMenuCta" style="margin-top:1rem;">Start Free →</button>
    </div>

    <!-- HERO -->
    <section class="rg-hero" id="home">
      <div class="rg-hero-bg">
        <div class="rg-orb rg-orb1"></div>
        <div class="rg-orb rg-orb2"></div>
        <div class="rg-orb rg-orb3"></div>
        <div class="rg-grain"></div>
      </div>
      <div class="rg-hero-inner">
        <div class="rg-badge">✨ AI-Powered Room Analysis — Trusted by 10,000+ users</div>
        <h1>What Score Does Your Room Deserve? 🏠</h1>
        <p class="rg-hero-sub">Discover your Room's Aesthetic Score in 30 seconds — then get AI-powered tips and shoppable products to transform your space</p>
        <div class="rg-hero-ctas">
          <button type="button" class="rg-btn-primary" id="rgHeroAnalyze">Get My Room Score Free →</button>
          <button type="button" class="rg-btn-outline" id="rgHeroExamples">See Real Examples 🔥</button>
        </div>
        <p class="rg-live-counter" id="rgLiveCounterLine">🔥 <span id="rgLiveNum">100</span> rooms analyzed today</p>
        <div class="rg-hero-mockup" aria-hidden="true">
          <div class="rg-mockup-glow"></div>
          <div class="rg-mockup-card">
            <div class="rg-mockup-score">
              <span class="rg-mockup-num">73<span class="rg-mockup-den">/100</span></span>
            </div>
            <div class="rg-mockup-pill">✨ Warm Minimalist</div>
            <div class="rg-mockup-tips">
              <div class="rg-mockup-tip">💡 Add warm accent lighting for instant coziness</div>
              <div class="rg-mockup-tip">🪴 Layer textures — a plant softens hard edges</div>
            </div>
          </div>
        </div>
        <div class="rg-social-proof">
          <div class="rg-avatars">
            <span class="rg-avatar"></span><span class="rg-avatar"></span><span class="rg-avatar"></span><span class="rg-avatar"></span><span class="rg-avatar"></span>
          </div>
          <span>Join 10,000+ home enthusiasts transforming their spaces</span>
        </div>
      </div>
      <div class="rg-scroll-hint" aria-hidden="true">↓</div>
    </section>

    <!-- TRUST -->
    <div class="rg-trust">
      <div class="rg-trust-shimmer"></div>
      <div class="rg-trust-row container">
        <div class="rg-trust-item">⚡ Instant AI Analysis</div>
        <span class="rg-trust-div"></span>
        <div class="rg-trust-item">🔒 Photos Never Stored</div>
        <span class="rg-trust-div"></span>
        <div class="rg-trust-item">⭐ 4.9/5 Average Rating</div>
        <span class="rg-trust-div"></span>
        <div class="rg-trust-item">🏠 10,000+ Rooms Analyzed</div>
        <span class="rg-trust-div"></span>
        <div class="rg-trust-item">🏆 Featured on Product Hunt</div>
      </div>
    </div>

    <!-- HOW -->
    <section class="rg-section section-alt" id="how">
      <div class="container">
        <span class="rg-pill-label">Simple Process</span>
        <h2 class="rg-sec-title">From Photo to Perfect Room in Seconds 🎯</h2>
        <p class="rg-sec-sub">Four quick steps from upload to your personalized score and shopping list.</p>
        <div class="rg-steps-wrap">
          <div class="rg-steps">
            <div class="rg-step-card rg-reveal" data-reveal>
              <div class="rg-step-num">01</div>
              <div class="rg-step-icon">📸</div>
              <h3>Upload Your Photo</h3>
              <p>Take a photo of any room, desk, closet or living space</p>
            </div>
            <div class="rg-step-card rg-reveal" data-reveal>
              <div class="rg-step-num">02</div>
              <div class="rg-step-icon">🛋️</div>
              <h3>Choose Room Type</h3>
              <p>Select your room type for more accurate personalized analysis</p>
            </div>
            <div class="rg-step-card rg-reveal" data-reveal>
              <div class="rg-step-num">03</div>
              <div class="rg-step-icon">🧠</div>
              <h3>AI Analyzes Everything</h3>
              <p>Our AI examines lighting, layout, color, furniture and style</p>
            </div>
            <div class="rg-step-card rg-reveal" data-reveal>
              <div class="rg-step-num">04</div>
              <div class="rg-step-icon">✨</div>
              <h3>Get Score + Shop Tips</h3>
              <p>Receive your Aesthetic Score, tips, and hand picked product links</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ANALYZER -->
    <section class="rg-section" id="analyze">
      <div class="container">
        <span class="rg-pill-label">Try It Free</span>
        <h2 class="rg-sec-title">Analyze Your Room Now 🏠</h2>
        <p class="rg-sec-sub">No account required. Get your first analysis free.</p>

        <div class="rg-room-row" id="rgRoomPills" role="group" aria-label="Room type">
          <button type="button" class="rg-room-pill is-active" data-room="Bedroom">🛏️ Bedroom</button>
          <button type="button" class="rg-room-pill" data-room="Living Room">🛋️ Living Room</button>
          <button type="button" class="rg-room-pill" data-room="Desk Setup">💻 Desk Setup</button>
          <button type="button" class="rg-room-pill" data-room="Closet">👗 Closet</button>
          <button type="button" class="rg-room-pill" data-room="Kitchen">🍳 Kitchen</button>
          <button type="button" class="rg-room-pill" data-room="Bathroom">🚿 Bathroom</button>
        </div>

        <div class="rg-upload-card" id="rgUploadCard">
          <div class="rg-skeleton" id="rgSkeleton"></div>
          <input type="file" id="rgFileInput" accept="image/*" hidden />
          <div class="rg-upload-card-inner" id="rgDropZone">
            <div class="rg-upload-placeholder" id="rgPlaceholder">
              <div style="font-size:3rem;margin-bottom:0.5rem;">📷</div>
              <p style="font-weight:700;font-size:18px;margin-bottom:0.5rem;">Drag & drop your photo here</p>
              <p style="color:var(--muted);font-size:14px;">or click to browse</p>
              <p style="color:var(--muted);font-size:12px;margin-top:1rem;">JPG, PNG, WEBP — up to 20MB</p>
            </div>
            <div class="rg-preview-wrap" id="rgPreviewWrap">
              <img id="rgPreviewImg" alt="Preview" />
              <button type="button" class="rg-change-photo" id="rgChangePhoto">Change Photo</button>
            </div>
          </div>
        </div>

        <button type="button" class="rg-analyze-btn rg-interactive" id="rgAnalyzeBtn" disabled>
          <span class="rg-btn-text">Analyze My Room ✨</span>
          <span class="rg-load-inner">
            <span class="rg-spinner"></span>
            <span class="rg-pulse-text">Analyzing your space...</span>
          </span>
        </button>
        <p class="rg-error" id="rgError"></p>

        <div class="rg-results" id="rgResults">
          <div class="rg-score-wrap">
            <div class="rg-score-ring" id="rgScoreRing" style="--pct:0%;--ring-color:#4caf50;">
              <span id="rgScoreNum">0</span>
            </div>
            <div class="rg-style-pill" id="rgStylePill">✨ Style</div>
            <p class="rg-hint">Here's how to improve your space:</p>
          </div>
          <div class="rg-tip-list" id="rgTipList"></div>

          <div class="rg-share-card">
            <div class="rg-logo-small">🔥 RoomGlow</div>
            <p style="color:var(--muted);font-size:14px;">Analyzed by RoomGlow AI</p>
            <p id="rgShareSummary" style="margin-top:0.75rem;font-weight:600;"></p>
            <div class="rg-share-actions">
              <button type="button" class="rg-btn-white" id="rgShareBtn">Share Your Score 📤</button>
              <button type="button" class="rg-btn-ghost" id="rgAnotherBtn">Analyze Another Room 🔄</button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- EXAMPLES -->
    <section class="rg-section section-alt" id="examples">
      <div class="container">
        <span class="rg-pill-label">Real Results</span>
        <h2 class="rg-sec-title">See What RoomGlow Discovers 🔥</h2>
        <p class="rg-sec-sub">Real rooms analyzed by our AI — see the scores and tips</p>
        <div class="rg-filters" id="rgExFilters">
          <button type="button" class="rg-filter-pill is-active" data-filter="all">All 🏠</button>
          <button type="button" class="rg-filter-pill" data-filter="Bedroom">Bedroom 🛏️</button>
          <button type="button" class="rg-filter-pill" data-filter="Living Room">Living Room 🛋️</button>
          <button type="button" class="rg-filter-pill" data-filter="Desk Setup">Desk 💻</button>
          <button type="button" class="rg-filter-pill" data-filter="Closet">Closet 👗</button>
        </div>
        <div class="rg-ex-grid" id="rgExGrid" style="position:relative;">
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Bedroom">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#667eea,#764ba2);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">🛏️</span>
              <span class="rg-badge-score">82/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Scandinavian Minimal</span>
              <span style="font-size:12px;color:var(--muted);">Bedroom</span>
            </div>
          </article>
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Living Room">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#f093fb,#f5576c);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">🛋️</span>
              <span class="rg-badge-score">71/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Boho Chic</span>
              <span style="font-size:12px;color:var(--muted);">Living Room</span>
            </div>
          </article>
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Desk Setup">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#4facfe,#00f2fe);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">💻</span>
              <span class="rg-badge-score">91/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Dark Academia</span>
              <span style="font-size:12px;color:var(--muted);">Desk Setup</span>
            </div>
          </article>
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Closet">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#43e97b,#38f9d7);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">👗</span>
              <span class="rg-badge-score">65/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Clean Aesthetic</span>
              <span style="font-size:12px;color:var(--muted);">Closet</span>
            </div>
          </article>
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Bedroom">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#fa709a,#fee140);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">🛏️</span>
              <span class="rg-badge-score">88/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Cottagecore</span>
              <span style="font-size:12px;color:var(--muted);">Bedroom</span>
            </div>
          </article>
          <article class="rg-ex-card rg-reveal" data-reveal data-room="Living Room">
            <div class="rg-ex-img" style="background:linear-gradient(135deg,#a18cd1,#fbc2eb);position:relative;">
              <span class="rg-ex-icon" aria-hidden="true">🛋️</span>
              <span class="rg-badge-score">77/100</span>
              <div class="rg-ex-overlay">See Tips</div>
            </div>
            <div class="rg-ex-meta">
              <span style="font-size:12px;padding:0.25rem 0.5rem;border-radius:var(--pill);background:#1a1a1a;border:1px solid var(--border);">Modern Industrial</span>
              <span style="font-size:12px;color:var(--muted);">Living Room</span>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- PRODUCTS -->
    <section class="rg-section" id="shop">
      <div class="container">
        <span class="rg-pill-label">Shop The Look</span>
        <h2 class="rg-sec-title">Products Our AI Recommends 🛍️</h2>
        <p class="rg-sec-sub">Hand picked items that transform real rooms — click to shop on Amazon</p>
        <div class="rg-prod-grid">
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#f7971e,#ffd200);">💡</div>
            <div class="rg-prod-body">
              <h4>Floor Lamp</h4>
              <p>Perfect for cozy bedroom lighting</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=warm+led+floor+lamp&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#56ab2f,#a8e063);">🌿</div>
            <div class="rg-prod-body">
              <h4>Desk Plant</h4>
              <p>Adds life and freshness to any desk</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=minimalist+desk+plant&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#ee9ca7,#ffdde1);">🛋️</div>
            <div class="rg-prod-body">
              <h4>Throw Blanket</h4>
              <p>Instant texture and warmth to sofas</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=aesthetic+throw+blanket&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#c94b4b,#4b134f);">📚</div>
            <div class="rg-prod-body">
              <h4>Wall Shelves</h4>
              <p>Clean storage that looks beautiful</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=floating+wall+shelves&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#f6d365,#fda085);">🕯️</div>
            <div class="rg-prod-body">
              <h4>Candle Set</h4>
              <p>Sets the mood in any room instantly</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=scented+candle+set&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
          <article class="rg-prod-card rg-interactive rg-reveal" data-reveal>
            <div class="rg-prod-emoji" style="background:linear-gradient(135deg,#89f7fe,#66a6ff);">🧺</div>
            <div class="rg-prod-body">
              <h4>Storage Basket</h4>
              <p>Organizes clutter stylishly</p>
              <a class="rg-btn-primary rg-amazon-link" style="padding:0.5rem 1rem;font-size:13px;" href="https://www.amazon.com/s?k=woven+storage+basket&amp;tag=technova050-20" target="_blank" rel="noopener">Shop This →</a>
            </div>
          </article>
        </div>
        <p style="text-align:center;color:var(--muted);margin-top:2rem;font-size:14px;">💡 RoomGlow recommends products based on your specific room analysis</p>
      </div>
    </section>

    <!-- STATS -->
    <section class="rg-section section-muted" id="stats">
      <div class="container">
        <div class="rg-stats-row">
          <div class="rg-reveal" data-reveal>
            <div class="rg-stat-num" data-target="10000" data-suffix="+">0</div>
            <div class="rg-stat-label">🏠 Rooms Analyzed</div>
          </div>
          <div class="rg-reveal" data-reveal>
            <div class="rg-stat-num" data-target="4.9" data-decimal="1">0</div>
            <div class="rg-stat-label">⭐ User Rating</div>
          </div>
          <div class="rg-reveal" data-reveal>
            <div class="rg-stat-num" data-target="2" data-prefix="$" data-suffix="M+">0</div>
            <div class="rg-stat-label">🛍️ Products Recommended</div>
          </div>
          <div class="rg-reveal" data-reveal>
            <div class="rg-stat-num" data-target="95" data-suffix="%">0</div>
            <div class="rg-stat-label">✨ Say Their Room Improved</div>
          </div>
        </div>
      </div>
    </section>

    <!-- TESTIMONIALS -->
    <section class="rg-section section-alt" id="reviews">
      <div class="container">
        <span class="rg-pill-label">Love Stories</span>
        <h2 class="rg-sec-title">People Are Obsessed 💫</h2>
        <p class="rg-sec-sub">Real people who transformed their spaces with RoomGlow</p>
      </div>
      <div class="rg-carousel-wrap">
        <div class="rg-carousel-track" id="rgCarouselTrack">
          <!-- duplicated for infinite loop -->
        </div>
      </div>
    </section>

    <!-- PRICING -->
    <section class="rg-section" id="pricing">
      <div class="container">
        <span class="rg-pill-label">Simple Pricing</span>
        <h2 class="rg-sec-title">Start Free, Upgrade When Ready 💎</h2>
        <p class="rg-sec-sub">No credit card required to start</p>
        <div class="rg-pricing-row">
          <div class="rg-pricing-card rg-interactive rg-reveal" data-reveal>
            <div style="font-weight:800;color:var(--gold);">Free</div>
            <div class="rg-price">$0</div>
            <p style="color:var(--muted);font-size:14px;">Forever free</p>
            <ul class="rg-features">
              <li>✅ 2 room analyses per day</li>
              <li>✅ Aesthetic score</li>
              <li>✅ 5 AI tips per analysis</li>
              <li>✅ Product recommendations</li>
              <li>✅ Shareable score card</li>
            </ul>
            <button type="button" class="rg-btn-outline" style="width:100%;" id="rgPricingFree">Start Analyzing Free</button>
          </div>
          <div class="rg-pricing-card rg-popular rg-interactive rg-reveal" data-reveal>
            <span class="rg-pop-badge">Most Popular</span>
            <div style="font-weight:800;color:var(--gold);">Pro</div>
            <div class="rg-price">$7.99<span style="font-size:16px;font-weight:600;">/month</span></div>
            <p style="color:var(--muted);font-size:14px;">Cancel anytime</p>
            <ul class="rg-features">
              <li>✅ Unlimited analyses</li>
              <li>✅ Advanced aesthetic score</li>
              <li>✅ 10 detailed tips per analysis</li>
              <li>✅ Style identity profile</li>
              <li>✅ Progress tracking over time</li>
              <li>✅ Priority AI processing</li>
              <li>✅ Early access to new features</li>
            </ul>
            <button type="button" class="rg-nav-cta" style="width:100%;border:none;" id="rgUpgradePro">Upgrade to Pro →</button>
            <p style="text-align:center;color:var(--muted);font-size:12px;margin-top:0.75rem;">🔒 Secure payment via Stripe</p>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="rg-section section-alt" id="faq">
      <div class="container">
        <span class="rg-pill-label">FAQ</span>
        <h2 class="rg-sec-title">Questions? We Got Answers 💬</h2>
        <div class="rg-faq-list">
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">How accurate is the AI room analysis? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">Our AI is powered by GPT-4o Vision, one of the most advanced image understanding models available. It analyzes lighting, color harmony, furniture placement, clutter, and style consistency to give you a precise and actionable score.</div></div>
          </div>
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">Is my photo stored or shared anywhere? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">Never. Your photos are sent directly to the AI for analysis and immediately discarded. We do not store, share, or use your photos for any other purpose. Your privacy is our priority.</div></div>
          </div>
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">How many free analyses do I get? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">Every visitor gets 2 free analyses per day with no account required. Upgrade to Pro for unlimited daily analyses and advanced features.</div></div>
          </div>
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">What rooms work best? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">RoomGlow works on any indoor space — bedrooms, living rooms, desk setups, closets, kitchens, and bathrooms. The clearer and better lit your photo, the more accurate your results.</div></div>
          </div>
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">How do the product recommendations work? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">After analyzing your room, our AI suggests specific types of products that would improve your aesthetic score. Each tip includes a direct Amazon search link so you can shop instantly.</div></div>
          </div>
          <div class="rg-faq-item">
            <button type="button" class="rg-faq-q">Can I track my room's improvement over time? <span>+</span></button>
            <div class="rg-faq-a"><div class="rg-faq-a-inner">Pro users get access to progress tracking, so you can see your aesthetic score improve as you make changes to your space.</div></div>
          </div>
        </div>
      </div>
    </section>

    <!-- FINAL CTA -->
    <section class="rg-final-cta" id="final-cta">
      <h2>What Score Does Your Room Deserve? 🏠</h2>
      <p>Find out in 30 seconds. No account needed.</p>
      <button type="button" class="rg-btn-white" id="rgFinalCta">Get My Room Score Free →</button>
      <p style="font-size:14px;margin-top:0.75rem;color:rgba(10,10,10,0.75);">✨ Join 10,000+ people who already discovered their room's potential</p>
    </section>

    <!-- FOOTER -->
    <footer class="rg-footer">
      <div class="rg-footer-top container">
        <div class="rg-logo">🔥 RoomGlow</div>
        <nav>
          <a href="#home" class="rg-anchor">Home</a>
          <a href="#how" class="rg-anchor">How It Works</a>
          <a href="#examples" class="rg-anchor">Examples</a>
          <a href="#pricing" class="rg-anchor">Pricing</a>
          <a href="#reviews" class="rg-anchor">Reviews</a>
        </nav>
        <p class="rg-tagline">Aesthetic intelligence for every room.</p>
      </div>
      <div class="rg-footer-bottom container">
        <span>© <span id="rgYear"></span> RoomGlow. All rights reserved.</span>
        <span>Made with ❤️ and AI</span>
        <span><a href="#privacy" class="rg-link">Privacy Policy</a> · <a href="#terms" class="rg-link">Terms</a></span>
      </div>
    </footer>

    <section id="privacy" class="rg-section section-muted" style="padding-top:3rem;padding-bottom:3rem;">
      <div class="container">
        <h2 class="rg-sec-title" style="font-size:1.5rem;">Privacy</h2>
        <p class="rg-sec-sub" style="margin-bottom:0;">Photos are analyzed in real time and not stored. We only use your image to generate your score and tips.</p>
      </div>
    </section>
    <section id="terms" class="rg-section section-alt" style="padding-top:3rem;padding-bottom:4rem;">
      <div class="container">
        <h2 class="rg-sec-title" style="font-size:1.5rem;">Terms</h2>
        <p class="rg-sec-sub" style="margin-bottom:0;">RoomGlow is provided as-is for personal use. Product links may be affiliate links (tag=technova050-20).</p>
      </div>
    </section>

    <div class="rg-modal" id="rgProModal" role="dialog" aria-modal="true" aria-labelledby="rgProModalTitle" aria-hidden="true">
      <div class="rg-modal-backdrop" id="rgProModalBackdrop"></div>
      <div class="rg-modal-card">
        <button type="button" class="rg-modal-close" id="rgProModalClose" aria-label="Close">&times;</button>
        <h3 id="rgProModalTitle">Pro Plan Coming Soon 🚀</h3>
        <p>Coming Soon — Pro plan launching soon! Enter your email to be notified.</p>
        <p style="margin-top:0.5rem;">Be the first to know when Pro launches and get 50% off your first month</p>
        <form id="rgProForm" action="javascript:void(0)">
          <input type="email" id="rgProEmail" name="email" placeholder="you@email.com" autocomplete="email" required />
          <button type="submit" class="rg-nav-cta" style="width:100%;border:none;" id="rgProNotify">Notify Me →</button>
        </form>
        <p class="rg-modal-msg" id="rgProThanks">Thanks! We'll notify you 🎉</p>
      </div>
    </div>

    <div class="rg-toast" id="rgToast" role="status" aria-live="polite">Copied!</div>
  </div>

  <script>
    (function () {
      const BACKEND_URL = "https://roomglow-backend.onrender.com/analyze";
      const MAX_BYTES = 20 * 1024 * 1024;
      const NAV_OFFSET = 76;
      const AFFILIATE_TAG = "technova050-20";

      let currentBase64 = null;
      let currentMime = "image/jpeg";
      let selectedRoom = "Bedroom";
      let lastScore = 0;
      let lastStyle = "";

      const $ = (id) => document.getElementById(id);

      const fileInput = $("rgFileInput");
      const dropZone = $("rgDropZone");
      const uploadCard = $("rgUploadCard");
      const placeholder = $("rgPlaceholder");
      const previewWrap = $("rgPreviewWrap");
      const previewImg = $("rgPreviewImg");
      const analyzeBtn = $("rgAnalyzeBtn");
      const errorEl = $("rgError");
      const results = $("rgResults");
      const tipList = $("rgTipList");
      const skeleton = $("rgSkeleton");
      const scoreRing = $("rgScoreRing");
      const scoreNum = $("rgScoreNum");
      const stylePill = $("rgStylePill");
      const shareSummary = $("rgShareSummary");
      const nav = $("rgNav");
      const rgToast = $("rgToast");
      const liveNumEl = $("rgLiveNum");

      function showError(msg) {
        errorEl.textContent = msg || "Something went wrong. Please try again.";
        errorEl.classList.add("visible");
      }
      function clearError() {
        errorEl.textContent = "";
        errorEl.classList.remove("visible");
      }

      function scrollToId(id) {
        const el = document.getElementById(id);
        if (!el) return;
        const y = el.getBoundingClientRect().top + window.scrollY - NAV_OFFSET;
        window.scrollTo({ top: Math.max(0, y), behavior: "smooth" });
      }

      function scrollToEl(el) {
        if (!el) return;
        const y = el.getBoundingClientRect().top + window.scrollY - NAV_OFFSET;
        window.scrollTo({ top: Math.max(0, y), behavior: "smooth" });
      }

      function scrollToAnalyze() {
        scrollToId("analyze");
      }

      document.querySelectorAll('a[href^="#"]').forEach((a) => {
        const href = a.getAttribute("href");
        if (!href || href === "#" || href.length < 2) return;
        const id = href.slice(1);
        if (!document.getElementById(id)) return;
        a.addEventListener("click", (e) => {
          e.preventDefault();
          scrollToId(id);
        });
      });

      function withAmazonTag(url) {
        const raw = String(url || "").trim();
        if (!raw) return "https://www.amazon.com/s?k=room+decor&tag=" + AFFILIATE_TAG;
        try {
          const u = new URL(raw, window.location.origin);
          if (!/amazon\./i.test(u.hostname)) return raw;
          u.searchParams.set("tag", AFFILIATE_TAG);
          return u.toString();
        } catch (_) {
          if (raw.includes("tag=")) return raw;
          return raw + (raw.includes("?") ? "&" : "?") + "tag=" + AFFILIATE_TAG;
        }
      }

      function showToast(msg) {
        if (!rgToast) return;
        rgToast.textContent = msg || "Copied!";
        rgToast.classList.add("show");
        clearTimeout(showToast._t);
        showToast._t = setTimeout(() => rgToast.classList.remove("show"), 2200);
      }

      const liveStart = 89 + Math.floor(Math.random() * (127 - 89 + 1));
      if (liveNumEl) liveNumEl.textContent = String(liveStart);
      setInterval(() => {
        if (!liveNumEl) return;
        const n = parseInt(liveNumEl.textContent, 10) || liveStart;
        liveNumEl.textContent = String(n + 1);
      }, 45000);

      function setRoomPills() {
        const row = $("rgRoomPills");
        if (!row) return;
        row.querySelectorAll(".rg-room-pill").forEach((btn) => {
          btn.classList.toggle("is-active", btn.dataset.room === selectedRoom);
        });
        row.addEventListener("click", (e) => {
          const btn = e.target.closest(".rg-room-pill");
          if (!btn || !row.contains(btn)) return;
          selectedRoom = btn.dataset.room || "Bedroom";
          row.querySelectorAll(".rg-room-pill").forEach((b) => {
            b.classList.toggle("is-active", b.dataset.room === selectedRoom);
          });
        });
      }
      setRoomPills();

      document.querySelectorAll("#rgNavCta, #rgHeroAnalyze, #rgFinalCta, #rgPricingFree").forEach((el) => {
        el && el.addEventListener("click", scrollToAnalyze);
      });
      $("rgHeroExamples")?.addEventListener("click", () => scrollToId("examples"));

      // Mobile menu
      const overlay = $("rgMenuOverlay");
      $("rgMenuOpen")?.addEventListener("click", () => overlay.classList.add("open"));
      $("rgMenuClose")?.addEventListener("click", () => overlay.classList.remove("open"));
      overlay?.querySelectorAll("a[href^='#']").forEach((a) =>
        a.addEventListener("click", () => overlay.classList.remove("open"))
      );
      $("rgMenuCta")?.addEventListener("click", () => {
        overlay.classList.remove("open");
        scrollToAnalyze();
      });

      // Nav shrink
      window.addEventListener("scroll", () => {
        nav.classList.toggle("rg-nav--compact", window.scrollY > 40);
      }, { passive: true });

      function fileToBase64(file) {
        return new Promise((resolve, reject) => {
          const r = new FileReader();
          r.onload = () => {
            const s = String(r.result);
            const i = s.indexOf(",");
            resolve(i >= 0 ? s.slice(i + 1) : s);
          };
          r.onerror = reject;
          r.readAsDataURL(file);
        });
      }

      function handleFile(file) {
        clearError();
        if (!file || !file.type.startsWith("image/")) {
          showError("Please upload a valid image.");
          return;
        }
        if (file.size > MAX_BYTES) {
          showError("Image too large (max 20MB).");
          return;
        }
        currentMime = file.type || "image/jpeg";
        analyzeBtn.disabled = true;
        const reader = new FileReader();
        reader.onload = () => {
          previewImg.src = reader.result;
          placeholder.classList.add("hidden");
          previewWrap.classList.add("visible");
        };
        reader.readAsDataURL(file);
        results.classList.remove("visible");
        tipList.innerHTML = "";
        fileToBase64(file).then((b64) => {
          currentBase64 = b64;
          analyzeBtn.disabled = false;
        }).catch(() => showError("Could not read file."));
      }

      dropZone.addEventListener("click", (e) => {
        if (e.target.closest("#rgChangePhoto") || e.target.closest("button")) return;
        if (e.target.tagName === "IMG") return;
        fileInput.click();
      });
      $("rgChangePhoto")?.addEventListener("click", (e) => { e.stopPropagation(); fileInput.click(); });
      fileInput.addEventListener("change", () => {
        const f = fileInput.files && fileInput.files[0];
        if (f) handleFile(f);
      });
      ["dragenter", "dragover"].forEach((ev) => {
        uploadCard.addEventListener(ev, (e) => { e.preventDefault(); uploadCard.classList.add("rg-drag"); });
      });
      ["dragleave", "drop"].forEach((ev) => {
        uploadCard.addEventListener(ev, (e) => { e.preventDefault(); uploadCard.classList.remove("rg-drag"); });
      });
      uploadCard.addEventListener("drop", (e) => {
        const f = e.dataTransfer.files && e.dataTransfer.files[0];
        if (f) handleFile(f);
      });

      function scoreColor(score) {
        if (score <= 40) return "#e53935";
        if (score <= 70) return "#fbc02d";
        return "#4caf50";
      }

      function animateScore(target) {
        const dur = 1200;
        const start = performance.now();
        const from = 0;
        function frame(now) {
          const t = Math.min(1, (now - start) / dur);
          const ease = 1 - Math.pow(1 - t, 3);
          const val = Math.round(from + (target - from) * ease);
          scoreNum.textContent = val;
          scoreRing.style.setProperty("--pct", String(val));
          if (t < 1) requestAnimationFrame(frame);
        }
        requestAnimationFrame(frame);
      }

      function renderTips(tips) {
        tipList.innerHTML = "";
        const list = Array.isArray(tips) ? tips.slice(0, 5) : [];
        while (list.length < 5) {
          list.push({
            icon: "✨",
            category: "Tip",
            tip: "Keep refining your space — small changes add up.",
            product_search_url: "https://www.amazon.com/s?k=room+decor",
          });
        }
        list.forEach((tip, i) => {
          const card = document.createElement("div");
          card.className = "rg-tip-card rg-interactive";
          card.style.animationDelay = i * 200 + "ms";
          const icon = escapeHtml(tip.icon || "✨");
          const cat = escapeHtml(tip.category || "Tip");
          const txt = escapeHtml(tip.tip || "Personalize this corner of your room.");
          const tagged = withAmazonTag(tip.product_search_url || "https://www.amazon.com/s?k=room+decor");
          card.innerHTML = `
            <div class="rg-cat"><span class="rg-cat-pill">${cat}</span></div>
            <div class="rg-tip-icon">${icon}</div>
            <div>
              <p class="rg-tip-text">${txt}</p>
              <button type="button" class="rg-shop-btn" data-url="${escapeAttr(tagged)}">Shop This →</button>
            </div>
          `;
          tipList.appendChild(card);
        });
        tipList.querySelectorAll(".rg-shop-btn").forEach((btn) => {
          btn.addEventListener("click", () => {
            const u = withAmazonTag(btn.dataset.url);
            window.open(u, "_blank", "noopener,noreferrer");
          });
        });
      }

      function escapeHtml(s) {
        return String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
      }
      function escapeAttr(s) {
        return escapeHtml(s).replace(/'/g, "&#39;");
      }

      async function analyze() {
        if (!currentBase64) {
          showError("Please upload a photo first.");
          return;
        }
        clearError();
        skeleton.classList.add("active");
        analyzeBtn.disabled = true;
        analyzeBtn.classList.add("loading");
        results.classList.remove("visible");

        try {
          const res = await fetch(BACKEND_URL, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              image: currentBase64,
              mime: currentMime,
              room_type: selectedRoom,
            }),
          });
          let data;
          try {
            data = await res.json();
          } catch (_) {
            throw new Error("bad_json");
          }
          if (!res.ok || typeof data.score !== "number" || !Array.isArray(data.tips)) {
            throw new Error("api_fail");
          }
          const rawScore = Number(data.score);
          lastScore = Number.isFinite(rawScore) ? Math.min(100, Math.max(0, Math.round(rawScore))) : 0;
          lastStyle = String(data.style || "Your Style").trim() || "Your Style";
          scoreRing.style.setProperty("--ring-color", scoreColor(lastScore));
          stylePill.textContent = "✨ " + lastStyle;
          if (shareSummary) shareSummary.textContent = "Score " + lastScore + "/100 · " + lastStyle;
          results.classList.add("visible");
          animateScore(lastScore);
          renderTips(data.tips);
          scrollToEl(results);
        } catch (e) {
          showError("Something went wrong. Please try again.");
        } finally {
          skeleton.classList.remove("active");
          analyzeBtn.classList.remove("loading");
          analyzeBtn.disabled = !currentBase64;
        }
      }

      analyzeBtn.addEventListener("click", analyze);

      $("rgAnotherBtn")?.addEventListener("click", () => {
        results.classList.remove("visible");
        tipList.innerHTML = "";
        fileInput.value = "";
        currentBase64 = null;
        previewImg.removeAttribute("src");
        placeholder.classList.remove("hidden");
        previewWrap.classList.remove("visible");
        analyzeBtn.disabled = true;
        scrollToAnalyze();
      });

      $("rgShareBtn")?.addEventListener("click", async function shareScore() {
        const text = "My RoomGlow score: " + (lastScore || 0) + "/100 — " + (lastStyle || "RoomGlow");
        const url = window.location.href;
        if (navigator.share && /Android|iPhone|iPad|iPod/i.test(navigator.userAgent)) {
          try {
            await navigator.share({ title: "RoomGlow", text, url });
            return;
          } catch (err) {
            if (err && err.name === "AbortError") return;
          }
        }
        try {
          await navigator.clipboard.writeText(text + " " + url);
          showToast("Copied!");
        } catch (_) {
          try {
            const ta = document.createElement("textarea");
            ta.value = text + " " + url;
            document.body.appendChild(ta);
            ta.select();
            document.execCommand("copy");
            document.body.removeChild(ta);
            showToast("Copied!");
          } catch (__) {
            prompt("Copy this:", text + " " + url);
          }
        }
      });

      // IntersectionObserver reveals
      const io = new IntersectionObserver(
        (entries) => {
          entries.forEach((en) => {
            if (en.isIntersecting) en.target.classList.add("visible");
          });
        },
        { threshold: 0.12 }
      );
      document.querySelectorAll("[data-reveal]").forEach((el) => io.observe(el));

      function observeReveals(root) {
        root.querySelectorAll("[data-reveal]").forEach((el) => io.observe(el));
      }

      // Stats counter
      let statsDone = false;
      const statsIo = new IntersectionObserver(
        (entries) => {
          entries.forEach((en) => {
            if (!en.isIntersecting || statsDone) return;
            statsDone = true;
            en.target.querySelectorAll(".rg-stat-num").forEach((el) => {
              const target = parseFloat(el.dataset.target);
              const dec = el.dataset.decimal ? parseInt(el.dataset.decimal, 10) : 0;
              const prefix = el.dataset.prefix || "";
              const suffix = el.dataset.suffix || "";
              const dur = 1500;
              const start = performance.now();
              function tick(now) {
                const t = Math.min(1, (now - start) / dur);
                const ease = 1 - Math.pow(1 - t, 2);
                let v = target * ease;
                if (dec === 0) el.textContent = prefix + Math.round(v) + suffix;
                else el.textContent = prefix + v.toFixed(dec) + suffix;
                if (t < 1) requestAnimationFrame(tick);
              }
              requestAnimationFrame(tick);
            });
          });
        },
        { threshold: 0.3 }
      );
      const statsSec = $("stats");
      if (statsSec) statsIo.observe(statsSec);

      // FAQ
      document.querySelectorAll(".rg-faq-q").forEach((btn) => {
        btn.addEventListener("click", () => {
          const item = btn.closest(".rg-faq-item");
          const wasOpen = item.classList.contains("open");
          document.querySelectorAll(".rg-faq-item").forEach((i) => {
            i.classList.remove("open");
            const s = i.querySelector(".rg-faq-q span");
            if (s) s.textContent = "+";
          });
          if (!wasOpen) {
            item.classList.add("open");
            const s = btn.querySelector("span");
            if (s) s.textContent = "−";
          }
        });
      });

      // Example filters (fade then hide)
      const exFilters = $("rgExFilters");
      const exGrid = $("rgExGrid");
      exFilters?.addEventListener("click", (e) => {
        const pill = e.target.closest(".rg-filter-pill");
        if (!pill) return;
        exFilters.querySelectorAll(".rg-filter-pill").forEach((p) => p.classList.remove("is-active"));
        pill.classList.add("is-active");
        const f = pill.dataset.filter;
        const cards = document.querySelectorAll(".rg-ex-card");
        cards.forEach((card) => {
          const show = f === "all" || card.dataset.room === f;
          if (show) {
            card.classList.remove("rg-hidden", "rg-ex-hiding");
          } else {
            card.classList.add("rg-ex-hiding");
          }
        });
        clearTimeout(exFilters._ft);
        exFilters._ft = setTimeout(() => {
          cards.forEach((card) => {
            const show = f === "all" || card.dataset.room === f;
            if (!show) card.classList.add("rg-hidden");
            else card.classList.remove("rg-ex-hiding");
          });
          if (exGrid) exGrid.style.minHeight = "";
        }, 320);
      });

      // Testimonials carousel — duplicate nodes
      const testimonials = [
        { q: "I got a 43/100 on my bedroom and was honestly a little offended 😂 But I followed every tip and bought the lamp it suggested and now I'm at 78. My room looks incredible.", n: "Sarah K.", r: "College Student", g: "linear-gradient(135deg,#ff9a56,#ff6a88)" },
        { q: "The AI told me my desk setup was 'missing warmth and personality' and recommended a plant and a LED strip. Spent $40, now my setup looks like it belongs on Reddit.", n: "Marcus T.", r: "Software Developer", g: "linear-gradient(135deg,#f7971e,#ffd200)" },
        { q: "I used this on my living room before having people over for a dinner party. Followed 3 of the 5 tips. My friend literally asked if I hired an interior designer.", n: "Priya M.", r: "Marketing Manager", g: "linear-gradient(135deg,#f093fb,#f5576c)" },
        { q: "The aesthetic style detection is scary accurate. It called my room 'Dark Academia with Maximalist tendencies' which is literally my entire personality.", n: "Jake R.", r: "Photographer", g: "linear-gradient(135deg,#fa709a,#fee140)" },
        { q: "As a college student on a budget this is gold. The product recommendations are all affordable and actually match my room. I've bought 4 things it suggested.", n: "Emma L.", r: "University Student", g: "linear-gradient(135deg,#a8edea,#fed6e3)" },
        { q: "I run an Airbnb and I use RoomGlow on every room before listing. My bookings went up after I renovated based on the tips. Genuinely useful tool.", n: "David C.", r: "Airbnb Host", g: "linear-gradient(135deg,#d4a574,#8b4513)" },
      ];
      const track = $("rgCarouselTrack");
      function initialsFromName(name) {
        const parts = String(name || "").replace(/\./g, " ").trim().split(/\s+/).filter(Boolean);
        if (!parts.length) return "?";
        const a = parts[0][0] || "";
        const b = parts.length > 1 ? parts[parts.length - 1][0] : (parts[0][1] || "");
        return (a + b).toUpperCase();
      }
      function buildTestiCard(t) {
        const div = document.createElement("div");
        div.className = "rg-testi-card rg-interactive rg-reveal";
        div.setAttribute("data-reveal", "");
        const ini = initialsFromName(t.n);
        const grad = t.g || "linear-gradient(135deg,#f0a500,#ff6b35)";
        div.innerHTML = `<div class="rg-stars">★★★★★</div><blockquote>${escapeHtml(t.q)}</blockquote><div class="rg-author"><div class="rg-testi-av" style="background:${grad};">${escapeHtml(ini)}</div><div><strong>${escapeHtml(t.n)}</strong><br/><span style="color:var(--muted);font-size:13px;">${escapeHtml(t.r)}</span></div></div>`;
        return div;
      }
      testimonials.forEach((t) => track.appendChild(buildTestiCard(t)));
      testimonials.forEach((t) => track.appendChild(buildTestiCard(t)));
      if (track) observeReveals(track);

      document.querySelectorAll("a.rg-amazon-link").forEach((a) => {
        try {
          a.href = withAmazonTag(a.getAttribute("href"));
        } catch (_) {}
      });

      const proModal = $("rgProModal");
      const openPro = () => {
        if (!proModal) return;
        proModal.classList.add("open");
        proModal.setAttribute("aria-hidden", "false");
        const th = $("rgProThanks");
        const fm = $("rgProForm");
        if (th) th.style.display = "none";
        if (fm) fm.style.display = "";
        const em = $("rgProEmail");
        if (em) em.value = "";
      };
      const closePro = () => {
        if (!proModal) return;
        proModal.classList.remove("open");
        proModal.setAttribute("aria-hidden", "true");
      };
      $("rgUpgradePro")?.addEventListener("click", openPro);
      $("rgProModalClose")?.addEventListener("click", closePro);
      $("rgProModalBackdrop")?.addEventListener("click", closePro);
      $("rgProForm")?.addEventListener("submit", (ev) => {
        ev.preventDefault();
        const thanks = $("rgProThanks");
        const form = $("rgProForm");
        if (thanks) thanks.style.display = "block";
        if (form) form.style.display = "none";
      });
      document.addEventListener("keydown", (e) => {
        if (e.key === "Escape" && proModal && proModal.classList.contains("open")) closePro();
      });

      $("rgYear").textContent = new Date().getFullYear();
    })();
  </script>
</body>
</html>
